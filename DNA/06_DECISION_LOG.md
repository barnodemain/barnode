# Decision log

Decisioni tecniche rilevanti su BARnode. Solo scelte che un agent futuro deve conoscere per non rifarle o contraddirle. Le REGOLE di lavoro stanno in `CLAUDE.md` (governance), non qui.

| Data | Decisione | Motivo |
|------|-----------|--------|
| 2026-07-22 | **Backup interno esteso al ricettario + consolidamento post-audit.** (1) `backupService`: payload snapshot ora include `cocktails`/`cocktail_ingredients`/`preparations`/`preparation_ingredients` (colonne complete, id inclusi) + guard anti-sovrascrittura se il DB legge vuoto; `useRecipeAdmin` chiama lo snapshot post save/delete come gli altri hook. Migrazione additiva `20260722_backup_ricettario.sql`: `restore_last_backup` ricreata (drop+create, stesso nome/zero argomenti) — ripristina il ricettario SOLO se presente nel payload (retro-compatibile con snapshot vecchi); ordine FK rispettato. **La migrazione va applicata a mano nel SQL Editor** (rete locale senza IPv6 → niente accesso diretto al DB; pooler non attivo sul progetto). Finché non applicata: snapshot già completo, ripristino limitato a catalogo+mancanti come prima (nessuna rottura). (2) Pulizia residui CERTI da audit: `findByName` (useArticoli), CSS morti `.settings-button`/`.pin-modal-subtitle`/`.recipe-form-grid`, mock `/notes` nei test. (3) README riscritto (italiano, feature attuali, senza SQL duplicato — rimanda a `supabase/migrations/`). (4) Aggiunto `.env.example` (solo nomi variabili). (5) `npm audit fix`: brace-expansion (dev-only) → 0 vulnerabilità. | Esito dell'audit consolidamento: unico rischio concreto era il ripristino che non copriva le ricette. Verificato: lint, typecheck, build, 16 E2E. |
| 2026-07-22 | **Home: rimosso il FAB "+".** In Home il "+" creava solo un articolo a catalogo senza aggiungerlo ai mancanti (stessa funzione dell'Archivio) → confondeva. Rimosso FAB + relativo modale "Aggiungi articolo" da `pages/Home.tsx` e ripulito il codice morto (import `Modal`/`FloatingActionButton`/`normalizeArticleName`, stati `isModalOpen`/`newItemName`/`isSubmitting`, `handleAddNewItem`, e `createArticolo`/`findByName` dall'hook). L'aggiunta ai mancanti dalla Home resta via autocomplete/suggerimenti; la creazione articoli resta nel FAB Archivio. | Richiesta utente: il "+" in Home confonde. Nessuna modifica DB. Verificato: typecheck, build, 8 E2E (i test navigation Home continuano a passare). |
| 2026-07-22 | **Ricettario: Procedimento (form preparazione) editabile in un editor a tutta pagina.** Il campo Procedimento era una textarea inline `rows={4}` scomoda per testi lunghi. Ora è un box cliccabile (`.textarea-open`, anteprima troncata 4 righe) che apre `FullscreenTextEditor` (`components/recipes/FullscreenTextEditor.tsx`, `.text-editor-overlay` z-index 1100): textarea a tutta pagina, header Annulla/Procedimento/Fatto. Modifica una copia `draft`: Fatto conferma nel form, Annulla scarta. Componente riusabile (anche per Metodo cocktail). **A-capo e punti conservati** ovunque (editor nativo, anteprima `pre-line`, scheda `.prep-procedure` `pre-line`); testo allineato a sinistra (default). Nessuna modifica DB/salvataggio. | Richiesta utente: gestire meglio il testo lungo del procedimento con un modale a tutta pagina. Verificato: typecheck, build, 8 E2E + spec temporaneo dedicato (tap apre editor col testo, Annulla scarta / Fatto conferma, round-trip multi-riga "Riga uno.\nRiga due.\nRiga tre finale." conservato, anteprima `white-space:pre-line`). Spec temporaneo rimosso. |
| 2026-07-22 | **Ricettario: preparazione nel garnish INLINE nel campo (testo verde + ×, no icona, no pillola).** Iterazione della soluzione chip: invece di un chip separato SOTTO il campo, la preparazione è ora **dentro** il campo (`.garnish-field` = contenitore con bordo bianco tipo `.modal-input`), come testo verde grassetto non modificabile (`.garnish-prep-inline`, nome in `<span>` + × alla fine, senza icona foglio e senza contorno) affiancato all'input di testo libero (`.garnish-input-inline`, senza bordo). Più user-friendly: l'utente vede il campo unico e capisce che può scrivere prima/dopo la preparazione. Logica invariata rispetto al chip (split/join su stringa `garnish`, nessuna colonna DB, bottone "+ Preparazione" nascosto se già collegata, dirty-check con `garnishPrep`). Rimossi CSS morti `.garnish-input`/`.garnish-prep-chip*`. **`joinGarnish` unisce con uno spazio, NON con la virgola** (richiesta utente); dato Piper ripulito nel DB ("Aria di St Germain, pepe rosa"→"Aria di St Germain pepe rosa"). | Richiesta utente: preparazione dentro il campo, testo verde con solo × alla fine, senza icona → più chiaro dove aggiungere testo libero. Verificato: typecheck, build, 8 E2E + spec temporaneo dedicato (pillola inline "Aria di St Germain" + input "pepe rosa" nello stesso `.garnish-field`, 1 solo svg = la ×, × rimuove senza toccare il testo). Spec temporaneo rimosso. |
| 2026-07-22 | **Ricettario: preparazione nel garnish come chip NON modificabile (solo rimuovibile).** Prima il picker inseriva il nome come testo libero → l'utente poteva modificarlo e rompere il collegamento. Ora la preparazione collegata è un chip verde separato (`.garnish-prep-chip`: icona doc + nome in `<span>` + × ) e il campo garnish contiene solo il testo libero. Scelto (con l'utente) di NON aggiungere colonna DB: si continua a riusare la stringa `garnish`. `RecipeForms`: `splitGarnish(garnish, preparations)` all'apertura separa `{prep, rest}`, `joinGarnish(prep?.nome, rest)` al salvataggio ricompone "Nome, testo libero" (helper in `lib/recipeFormat.ts`). Bottone "+ Preparazione" nascosto quando c'è già un chip (una prep per garnish). Snapshot dirty-check esteso con `garnishPrep`. **⚠️ Iterato subito dopo → versione inline (riga sopra).** | Richiesta utente: il nome della preparazione nel garnish non dev'essere modificabile (solo eliminabile) per non corrompere la logica; evidenziarlo. Verificato: typecheck, build, 8 E2E + 2 spec temporanei dedicati. |
| 2026-07-22 | **Ricettario: rimosso il badge/chip categoria dal bottom-sheet preparazione** (`PreparationDetail`: tolto `<span className="prep-category-chip">`; rimossa la regola CSS `.prep-category-chip` ormai morta). La `categoria` resta usata solo per raggruppare la lista preparazioni e nel form. | Richiesta utente: nel dettaglio preparazione il badge verde (es. "ARIA") era ridondante. Verificato: typecheck, build, 8 E2E. |
| 2026-07-22 | **Ricettario: inserimento preparazione nel garnish via picker (campo resta testo libero).** Sotto il campo garnish un bottone compatto "+ Preparazione" (`.garnish-add-prep`, `width: fit-content`, ~145px) apre il bottom-sheet delle preparazioni e inserisce il NOME esatto nel testo (vuoto→lo imposta; altrimenti coda con ", "). Il picker è stato **estratto** da `IngredientEditor` in un componente riusabile `components/recipes/PreparationPicker.tsx` (stesso markup/comportamento: ordine alfabetico, ricerca, `onPointerDown` anti doppio-tap) usato sia dagli ingredienti sia dal garnish → zero duplicazione. Il campo garnish resta visivamente identico (testo libero); il selettore serve solo a garantire che il nome coincida con la preparazione (→ icona ricetta sicura). | Richiesta utente: poter scrivere liberamente il garnish, ma inserire le preparazioni con un selettore così il nome è esatto. Fix discreto/non invasivo. Verificato: typecheck, build, 8 E2E + spec temporaneo dedicato (bottone visibile/compatto 145px su 333px, picker con 18 prep, inserimento "Peel di arancio"→"Peel di arancio, Aria di mezcal", nessuna scrittura DB), spec temporaneo rimosso. |
| 2026-07-22 | **Ricettario: icona "ricetta" anche nel garnish quando cita una preparazione.** Il campo garnish è testo libero (niente `preparation_id`): scelto (con l'utente) di NON toccare lo schema DB ma di riconoscere a runtime la preparazione citata. Nuova funzione `findPreparationInText(text, preparations)` in `lib/recipeFormat.ts` (match del nome-prep normalizzato come blocco di parole nel garnish, a confine di parola, preferendo il più lungo; usa `normalizeForCompare`). `CocktailCard`: se il garnish cita una prep, il valore diventa tappabile con l'icona `IoDocumentTextOutline` verde (apre il bottom-sheet), come gli ingredienti collegati (`.cocktail-garnish-value.linked`). Regola dati: **il nome nel garnish dev'essere identico alla preparazione** → PATCH "Aria sapida di St Germain, pepe rosa"→"Aria di St Germain, pepe rosa" (Piper); Negroni Hotel già coincideva ("Aria di mezcal"). Backup `BACKUP/pre-garnish_22Luglio.json`. | L'utente ha notato preparazioni citate nei garnish senza l'icona. Coerenza con gli ingredienti. Verificato: typecheck, build, simulazione match su tutti i 17 garnish reali = icona solo su Piper/Negroni Hotel, 0 falsi positivi. Nessun ciclo di import (analysisGrouping non importa recipeFormat). |
| 2026-07-22 | **Ricettario: tipo di ghiaccio da menu a tendina (selector), non più testo libero** — stesso pattern delle unità. Elenco unico `RECIPE_ICE` in `lib/recipeFormat.ts` (Ghiaccio a cubi, Cubo grande, Chunk, Ghiaccio tritato), approvato con l'utente. `RecipeForms`: campo Ghiaccio = `<select className="modal-input modal-select">` (freccia custom via `.modal-select`, coerente con `.ing-editor-unita`), prima opzione vuota **"-"** (campo lasciabile vuoto = nessun ghiaccio) + fallback per valori legacy fuori lista. Dato normalizzato via PATCH (solo campo `ghiaccio`): "Ghiaccio trito"→"Ghiaccio tritato". Backup pre-modifica in `BACKUP/pre-ghiaccio_22Luglio.json`. | Il ghiaccio era testo libero con un refuso ("trito"). Il selector rende impossibili refusi/incoerenze future. Verificato: typecheck OK. Dati: 17 cocktail invariati salvo l'unico "trito"→"tritato" previsto. |
| 2026-06-28 | `docs/` consolidata in `DNA/` (6 file numerati per priorità) | Standard enterprise; doc legacy disallineata dal codice |
| 2026-06-28 | Rimossi residui Replit (`.replit`, `replit.md`, `.local/`, alias `@assets`, `attached_assets/`); dev server su porta 5001 | Progetto migrato fuori da Replit; pulizia |
| 2026-06-28 | Pagina **Note** ripensata: da note libere sincronizzate → elenco prodotti sempre aggiornato dal DB (sola lettura, copiabile) | Scelta utente; risolve anche il bug delle 2 righe `notes` lette senza ordinamento |
| 2026-06-28 | Tabella `notes` **svuotata** (0 righe), struttura mantenuta | Non più usata dall'app dopo il ripensamento della pagina Note |
| 2026-06-28 | `npm audit fix` (no --force): 13 vuln → 0 | Erano tutte in devDependencies; nessun major cambiato |
| 2026-06-28 | Favicon = solo bicchiere (verde chiaro #4a9c3d, angoli arrotondati); logo completo per home/PWA | Il logo completo con testo è illeggibile a 16px nella scheda |
| 2026-06-28 | Keepalive Supabase via **GitHub Actions** (ping ogni 2gg) | Render Free non ha cron; GitHub Actions è gratis e vive nel repo. Era la causa delle pause Supabase. **⚠️ Superata il 2026-07-21 (vedi sotto): falliva in silenzio.** |
| 2026-07-21 | **Nuova sezione "Ricettario" (Cocktail + Preparazioni)** da PDF interno bartender. Dati su Supabase (4 tabelle, RLS anon CRUD) così l'utente le gestisce da solo; visibile a tutti dalla navbar (3a voce, icona martini); preparazioni home-made collegate agli ingredienti dei cocktail. Consultazione mobile-first (schede full-screen scroll-snap, bottom-sheet, ricerca) + gestione in Admin (`/settings/recipes`). Dati mantenuti integralmente dal PDF (campi mancanti vuoti). | Richiesta utente: ricettario digitale professionale, più pratico del PDF, consultabile durante il servizio. Layout stile app-ricette, non riproduzione del PDF (fatto male). Impatto Free trascurabile (~35 righe totali). Verificato: lint, build, 16 E2E, CRUD end-to-end con dato di test isolato (creato/verificato/eliminato), dati reali invariati (236/16). |
| 2026-07-22 | **Ricettario: unità di misura da menu a tendina (selector), non più testo libero.** Elenco unico `RECIPE_UNITS` in `lib/recipeFormat.ts` (13 unità minuscole, ordine per uso: oz, ml, lt, dash, drop, spoon, spray, top, gr, kg, pz, boccette, bt), approvate una a una con l'utente. `IngredientEditor`: campo unità = `<select>` con opzione vuota + fallback per valori legacy fuori lista. Dati esistenti normalizzati via PATCH mirati (solo campo `unita`, ricette NON alterate): L→lt, g→gr, teasp→spoon(≡bar spoon), sprz→spray, up/Up→top, Norm/filini→pz, Bottiglia→bt, "oz scarso"→oz + "scarso" spostato nella dose. Backup pre-conversione in `BACKUP/`. | Le unità erano scritte a mano con refusi/incoerenze (up/Up, oz scarso, ecc.). Il selector le rende impossibili in futuro. Verificato: lint, build, 16 E2E + spec dedicati (select con 13 opzioni, selezione funzionante). Integrità: 17 cocktail/18 prep/95+58 ingredienti invariati; confronto col backup = 0 dosi cambiate salvo l'unico "1/4"→"1/4 scarso" previsto. |
| 2026-07-22 | **Ricettario: protezione modifiche non salvate + scheda dopo salvataggio + rifiniture.** (1) Form (`RecipeForms`): se l'utente modifica e prova a chiudere col tap fuori dal modale, appare `ConfirmationDialog` "Modifiche non salvate" (Continua a modificare / Esci senza salvare) — non esce senza scelta esplicita. Snapshot dei valori all'apertura + confronto JSON per il dirty-check; se non ci sono modifiche, il tap fuori chiude subito. (2) Dopo il salvataggio di un cocktail, l'app porta in vista la sua scheda (`scrollToId` → `[data-cocktail-id]`), torna al tab Cocktail e pulisce la ricerca; `saveCocktail` ora ritorna l'id. (3) Cocktail sempre in **ordine alfabetico** (`useRecipes`, case/accent-insensitive). (4) Badge "ricetta" sull'ingrediente collegato = icona documento verde `IoDocumentTextOutline` (era pill testuale). (5) Selettore preparazioni: senza badge categoria, ordine alfabetico, selezione con `onPointerDown`+`preventDefault` (fix doppio-tap con tastiera). (6) Modale form `max-height: 82vh`; deck cocktail `scroll-snap-type: proximity` (scroll fluido). | Serie di richieste utente per rendere il ricettario enterprise/user-friendly. Nessuna modifica DB (solo UI/comportamento). Verificato: lint, typecheck, build, 16 E2E + spec dedicati (dirty-guard esci/continua, scheda in vista, selettore ordinato/senza badge, un-tap), dati invariati (17 cocktail, 18 prep, 236 articoli), 0 residui codice/dati, spec temporanei rimossi. |
| 2026-07-22 | **Dati: consolidamento ingredienti↔preparazioni nei cocktail.** Collegati e uniformati al nome esatto della preparazione (via PATCH mirati con backup per ognuno): 8 varianti "limone"→"Succo limone"; "Vintage"→"Prebatch Vintage"; "Grey Goose allo zafferano"→"Vodka zafferano"; + allineamento nomi già collegati (Cordiale salvia, Shrub pesca bianca [rinominata anche la prep], Soda pompelmo e timo [idem], Estratto cetriolo/basilico/zenzero, Cordiale anguria e lime, Soda pepe di Sichuan ×2). Risultato: **21/21 ingredienti collegati col nome identico alla preparazione**, 0 orfani. Scartati con conferma utente i falsi positivi (St Germain liquore≠aria, Zenzero fresco≠estratto, Essenza salvia≠cordiale, ecc.). | Refusi/varianti palesi: stesso prodotto scritto in modi diversi e non collegato → nelle schede non mostrava il badge "ricetta". Logica: i cocktail sono stati creati inserendo preparazioni esistenti, quindi il nome deve coincidere. Ogni modifica confermata dall'utente uno per uno, con backup in `BACKUP/`. |
| 2026-07-22 | **Ricettario: collegamento preparazione via bottone "+ Preparazione"** (nel form cocktail, accanto a "+ Ingrediente", stessa riga). Apre un bottom-sheet con l'elenco preparazioni + ricerca; la scelta viene aggiunta come normale riga ingrediente con `preparationId` già impostato (→ badge "ricetta" + bottom-sheet nella scheda). Scartato l'approccio iniziale (icona/autocomplete per riga + tag collegata/scollega): troppo complesso, l'utente ha chiesto la versione semplice "una volta inserita è un ingrediente come gli altri". Modale form abbassato a `max-height: 82vh` (si vedeva troppo pieno). Scroll deck cocktail da `scroll-snap-type: y mandatory` + `scroll-snap-stop: always` → **`proximity`** (rimosso lo stop): scroll fluido/naturale mobile invece dello scatto forzato scheda-per-scheda. | Richiesta utente: poter inserire le preparazioni esistenti in un cocktail senza riscriverle. `EditableIngredient.preparationId` già esisteva ma il form non lo impostava. Nessuna modifica DB. Verificato: lint, build, 16 E2E + spec dedicati (bottoni, selettore, crea-con-preparazione-collegata), dati reali invariati (cocktail 17, articoli 236, prep 18), 0 residui, spec temporanei rimossi. |
| 2026-07-22 | **Ricettario: form di modifica reso più usabile su mobile.** (1) Ogni ingrediente ora è una **card a due righe** (`IngredientEditor`): nome a tutta larghezza sopra, dose+unità+cestino sotto → i nomi lunghi ("Noilly Prat Dry Vermouth") non si tagliano più. Sfondo card beige scuro `#e6e3c3` per staccarle. (2) Bicchiere/Ghiaccio da griglia 2-col stretta a **full-width** uno sotto l'altro. (3) **Elimina/Annulla/Salva su una sola riga** (`.recipe-form-actions`): l'elimina è un cestino-icona (sfondo bianco, icona rossa, come i cestini ingrediente). (4) **Conferme eliminazione via `ConfirmationDialog`** (componente app esistente riusato) al posto di `confirm()` nativo, sia nel form sia nella lista Admin. (5) **Modale form allargato** (`.recipe-form` max-width 480px + margini overlay ridotti via `:has()` con fallback `@supports`/margin negativo per browser vecchi) → usa quasi tutta la larghezza su telefoni, resta 480px centrato su desktop. | Feedback utente: campi tagliati, troppi popup nativi, spazi vuoti ai margini. Layout non stravolto. Verificato: lint, build, 16 E2E + spec dedicati (screenshot leggibilità/azioni/conferma + CRUD reale crea/modifica/elimina con conferma app), conteggi DB invariati (cocktail 17, articoli 236, prep 18), 0 residui/orfani, spec temporanei rimossi. Compatibilità mobile Apple/Android verificata su viewport mobile+desktop. |
| 2026-07-21 | **Ricettario: gestione (CRUD) portata dentro la pagina Cocktail**, non più solo in Admin (i barman non usano Admin). Matita `LuPencilLine` in alto a dx su ogni scheda cocktail e su ogni bottom-sheet preparazione → apre il form; FAB "+" contestuale al tab per creare; **elimina in fondo al form** (scelta utente), non sulla scheda. Rimossa l'icona martini dal titolo scheda. I due form (cocktail+preparazione) estratti in un unico componente riusato `components/recipes/RecipeForms.tsx` (handle imperativo via ref) usato **sia** da Cocktail **sia** da RecipeAdmin → zero duplicazione di logica CRUD. Padding-bottom lista preparazioni aumentato perché il FAB non copra l'ultima card. | Richiesta utente: gestire ricette direttamente da Cocktail. Layout non stravolto. Icona matita "con riga" come da riferimento utente. Verificato: lint, build, 16 E2E + spec dedicati (UI + CRUD reale end-to-end crea/modifica/elimina) con dato di test isolato; conteggi DB invariati (cocktail 17→17, 0 residui/orfani), dati reali intatti (articoli 236, preparazioni 18). Spec temporanei rimossi. |
| 2026-07-21 | **Fix: rename articolo in Archivio ora si propaga a Home.** `updateArticolo` (`useArticoli.ts`), dopo l'update di `articoli`, aggiorna anche `missing_items.articolo_nome` sul DB (era una copia congelata) e la cache in-memory dei missing → Home si aggiorna subito, senza reload. | Bug segnalato: rinominando in Archivio, la lista Home mostrava il nome vecchio finché non si ricaricava l'app. La cache missing non veniva sincronizzata. Verificato con articolo di test isolato (creato/verificato/eliminato), 16 E2E, dati reali invariati. |
| 2026-07-21 | **Pulsante condivisione WhatsApp** in alto a sx nell'header di Home e Archivio (`components/ShareWhatsAppButton.tsx`): icona `IoLogoWhatsapp` verde brand, senza sfondo, area tocco 44×44. Apre `wa.me` col link pubblico `https://barnode-8gbl.onrender.com`. `.page-header-fixed` → `position: relative` per l'absolute del bottone. | Richiesta utente: condividere l'app. Componente unico riusato nelle 2 pagine (no duplicazione). Verificato lint+build+16 E2E. |
| 2026-07-21 | **Analysis rifatta: da "raggruppa per prima parola" a "somiglianza a coppie" (livello B).** Vecchia logica (categoria + stopwords) ammassava 172/249 articoli in 21 gruppi (59 vini, 21 gin…): puro rumore. Nuova (`similarityReason` + union-find): identici/parole-invertite/refusi/contenimento, con esclusione parole-categoria dal contenimento. Su archivio reale: da 21 gruppi-rumore → 7 coppie sensate. Etichetta "Motivo:". | La funzione non trovava doppioni ma categorie: inutilizzabile. Ora propone solo veri quasi-duplicati → archivio si mantiene pulito. Testato: 12 casi unit + 6 articoli finti isolati (creati, verificati visivamente, eliminati), 16 E2E, dati reali invariati (249/16). |
| 2026-07-21 | **Analysis: aggiunto criterio "contenimento per parole"** (categoria davanti/dietro). Il nome corto è un blocco di parole del lungo con 1-2 parole in più + una parola distintiva ≥5 char non-categoria. Cattura casi che l'esclusione-categoria bloccava ("Jagermeister"/"Amaro Jagermeister", "Noilly Prat"/"Vermouth Dry Noilly Prat") senza riprendere il rumore ("Succo Di Lime"/"Succo Di Mango" restano distinti). | Audit su archivio reale: 10 doppioni palesi "scritti con e senza categoria" non venivano proposti. Verificato: 14 coppie totali sensate, 0 rumore, 16 E2E + verifica visiva. |
| 2026-07-21 | **"Ignora" della Analysis reso persistente su DB** (tabella `ignored_pairs`, migrazione `20260721_ignored_pairs.sql`) al posto di `localStorage`. Chiave = `pair_key` (nomi normalizzati ordinati), stabile a rinomini/spostamenti. | Il vecchio ID gruppo dipendeva dalla composizione: cambiava a ogni modifica → gli "ignorati" riapparivano (bug segnalato dall'utente). Ora una coppia ignorata non ricompare mai, cross-device. Verificato E2E: ignora → reload → resta ignorata + riga nel DB. |
| 2026-07-21 | **Keepalive ridondante: principale = cron-job.org (giornaliero, con notifica email), riserva = GitHub Actions (giornaliero, 3 retry).** Il ping GitHub falliva dal 17/07 senza avvisare → DB andato in pausa il 21/07. cron-job.org è più puntuale, non si disattiva a 60gg e avvisa via email sui fallimenti. Per i **nuovi progetti**: solo cron-job.org, niente GitHub Actions. | Un solo guardiano cieco = punto singolo di rottura silenzioso. Due guardiani indipendenti + allarme email = da reazione a prevenzione. DB a 12MB/500 → i limiti Free non c'entravano, era pura inattività. |
| 2026-06-28 | Push via HTTPS + `GITHUB_TOKEN` (non SSH) | La chiave SSH non è autorizzata per l'owner `barnodemain` |
| 2026-06-28 | `Analysis.tsx` (526 righe) splittato in 4: `lib/analysisGrouping.ts` (logica pura), `hooks/useConsolidation.ts` (stato+handler), `components/AnalysisGroupCard.tsx` (UI card), `pages/Analysis.tsx` (114 righe, orchestrazione) | Governance: file sotto limite righe. Comportamento invariato (verificato lint+build+screenshot) |
| 2026-06-28 | Gestione **safe-area** solo sull'header (notch): `viewport-fit=cover` + `padding-top: env(safe-area-inset-top)` su `.page-header-fixed` | L'header rischiava di finire sotto il notch in PWA installata. Desktop invariato (safe-area=0). |
| 2026-06-28 | **Bottom-nav: NON usare `env(safe-area-inset-bottom)`.** `.bottom-nav` resta `bottom:0` + `height: var(--nav-height)` (70px) + icone centrate; `.item-list` padding-bottom 80px; `.fab` bottom = nav-height+20px. | Aggiungere la safe-area alla nav (commit 88a9c41) creava una grande fascia verde vuota sotto le icone in PWA su iPhone. La barra a `bottom:0` con altezza fissa è già corretta: le icone restano leggibili sopra la home-bar. Ripristinata la versione originale funzionante. **Non reintrodurre la safe-area sulla bottom-nav.** Verificato lint+build+16 E2E+screenshot. **⚠️ Superata il 2026-07-22 (vedi sotto): il modo CORRETTO di gestire la safe-area esiste.** |
| 2026-07-22 | **Bottom-nav in PWA: safe-area gestita correttamente (supera la voce del 28/06).** In PWA installata su iPhone, sotto la nav (70px fissi) traspariva lo sfondo crema nella fascia home-bar (nel browser no, la barra di Safari copre). Fix: `.bottom-nav` → `min-height: var(--nav-height)` (non `height`) + `padding-bottom: env(safe-area-inset-bottom)` + `align-items: flex-start`; `.bottom-nav-inner` → `height: var(--nav-height)` fisso. Così il **verde si estende sotto** la home-bar e **le icone restano nei 70px in alto** (NON spinte su). Nuova var `--nav-total-height = nav-height + safe-area` usata da `.fab`, `.item-list`, `.cocktail-deck`, `.prep-list-scroll` (padding-bottom) e safe-area su `.page-content-scrollable`. Fuori da PWA-iPhone `env()`=0 → tutto identico a prima. | La differenza con l'errore 88a9c41: quello metteva il padding SENZA aumentare l'altezza → icone spinte su + vuoto sopra. Questo aumenta `min-height` e tiene l'inner fisso → verde sotto, icone ferme. Verificato: lint, build, 16 E2E, screenshot nav con safe-area 0 (identica) e 34px (verde esteso, icone ferme, FAB alzato). **La regola aggiornata: la safe-area sulla bottom-nav si può usare, ma SOLO con questo pattern (min-height + inner fisso), mai come semplice padding su height fisso.** |
| 2026-06-28 | Suite **test E2E Playwright** (`tests/`, `playwright.config.ts`, script `test:e2e`): navigazione, flussi, screenshot mobile+desktop. Mock Supabase = zero scritture reali. 16 test passano | Progetto "già online" senza test: rete di sicurezza per modifiche future. Mobile su chromium (Pixel 5) per non scaricare WebKit. Esclusa dal bundle. |
| 2026-06-28 | **Cache condivisa** per articoli/missing (`lib/articoliStore.ts`, `lib/missingItemsStore.ts`): gli hook leggono dalla cache e fetchano solo se vuota; mutazioni aggiornano la cache + notificano le viste montate | Ogni cambio pagina rifaceva il fetch (misurato: 14 chiamate/3 navigazioni → 4, navigazioni successive 0). Consolidamento invalida la cache missing; restore backup fa reload (azzera cache). Verificato lint+build+16 E2E+sync test. |
| 2026-06-28 | **Hardening RLS** (`supabase/migrations/20260628_rls_hardening.sql`): sostituite le policy permissive `public/true/true` con policy `anon` mirate per operazione. `notes` → deny-all totale (vuota/inutilizzata). `articoli`/`missing_items` → CRUD anon con vincoli difensivi (nome 1-200 char, articolo_id non null). `backups_barnode` → solo INSERT/UPDATE + SELECT ristretto al solo record singleton (storico non enumerabile). Applicata via Management API. | Test penetrazione: chiunque con la chiave anon poteva insert/update/delete su tutte le tabelle. Senza login né backend (deploy unico, scelta utente) è il massimo ottenibile lato DB. Dati invariati (248+19). Verificato: write spazzatura bloccata, app funzionante, upsert backup OK. |

## Rischi noti / da valutare
- **Limite RLS senza login:** l'app scrive dal browser con la chiave anon, quindi `articoli`/`missing_items`/`ignored_pairs` restano insert/update/delete da chiunque abbia la chiave pubblica (mitigato da vincoli difensivi; `ignored_pairs` ha check su lunghezze e nessun UPDATE). Chiusura totale richiederebbe login Supabase o backend guardiano (Edge Function) — esclusi per non stravolgere il progetto / mantenere deploy unico. `notes` e storico `backups` invece sono chiusi.
- **PIN `1909`** hardcoded client-side ([PinModal.tsx](../src/components/PinModal.tsx)): protezione "soft", visibile nel bundle. Da rafforzare se serve sicurezza reale.
- **Bundle** `index` ~430KB (gzip ~127KB): normale per la SPA; code-splitting possibile se cresce.
- **GitHub Actions cron**: si disabilita dopo 60gg di inattività del repo (mitigato dai commit/deploy regolari **e dal keepalive ridondante cron-job.org**, che non ha questo limite).
- **App Control — limite scrittura agent:** dal canale agent (anon key + header) si può scrivere SOLO `project_env_variables` (es. `LINK_DEPLOY`). La tabella `projects` è in **sola lettura** via RLS: il campo `deploy_url` (link sotto il titolo nell'UI) NON è scrivibile dall'agent (la PATCH torna `*/0`), va messo a mano dall'interfaccia. Non perderci tempo via API.
